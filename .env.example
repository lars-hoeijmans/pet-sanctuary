# Copy to .env.local to override. All optional — the app runs with defaults.

# Active world source: "mock" (default, no backend) or "socket" (real server).
VITE_WORLD_SOURCE=mock

# Base URL of the Society Server (only used when VITE_WORLD_SOURCE=socket).
VITE_WORLD_SERVER_URL=http://localhost:3001
